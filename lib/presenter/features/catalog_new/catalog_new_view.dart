import 'package:flutter/material.dart';

class CatalogNewView extends StatelessWidget {
  const CatalogNewView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
